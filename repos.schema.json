{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "jupyter-workbench repos.json",
  "description": "Maps repo names to git URLs and optional package metadata.",
  "type": "object",
  "additionalProperties": {
    "type": "object",
    "required": ["url"],
    "properties": {
      "url": {
        "type": "string",
        "description": "Git clone URL."
      },
      "packages": {
        "type": "array",
        "description": "Package metadata. If absent, defaults: name = repo key with '-' replaced by '_', parentDir = '.'",
        "items": {
          "type": "object",
          "required": ["name", "parentDir"],
          "properties": {
            "name": {
              "type": "string",
              "description": "Python package name (used for 'jupyter server extension enable <name>')."
            },
            "parentDir": {
              "type": "string",
              "description": "Path from repo root to the directory containing pyproject.toml."
            }
          }
        }
      }
    }
  }
}
